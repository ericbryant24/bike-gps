// Built-in TomTom Search API key. A browser app cannot hide a key (it travels in
// every request), so this one is public by design: it's on the free tier with
// no payment method attached, and restricted in the TomTom portal to this
// site's domain. Settings → "TomTom key" overrides it on a device.
export const TOMTOM_KEY = 'fyuLrS1KIHVUdGsrs8A4z6O5RtkATAGi';
